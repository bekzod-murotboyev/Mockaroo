spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/maccaro
    username: postgres
    password: root123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: true
  messages:
    always-use-message-format: true
springdoc:
  swagger-ui:
    path: swagger-ui
  api-docs:
    path: /api-docs
server:
#  servlet:
#    context-path: /api
  port: 9090